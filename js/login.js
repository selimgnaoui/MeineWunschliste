function usercheck(user,pw) {
    var loginuser = user;
    var loginpw = pw;
    var checkuser = false;
    var checkuser = false;
    if (loginuser == checkuser) {
        if (loginpw == checkpw) {
            alert('Login ok');
            $_SESSION['user'] = loginuser;
            $_SESSION['logged'];
        }

    }
}